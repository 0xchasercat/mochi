# Path to the cookie-jar file (created on first run).
COOKIE_JAR=./state/example-cookies.json

# Origin of the application under test.
APP_ORIGIN=https://app.example.com

# Credentials for the interactive login fallback.
APP_EMAIL=me@example.com
APP_PASSWORD=
