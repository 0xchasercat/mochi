# Target URL — replace with the SPA you want to drive.
TARGET_URL=https://example.com/jobs?infinite

# Selector for each lazy-loaded item.
ITEM_SELECTOR=[data-testid=job-card]

# Hard cap on how many items to collect before stopping.
BUDGET_ITEMS=200

# Hard cap on total scroll time in milliseconds.
MAX_MS=90000
