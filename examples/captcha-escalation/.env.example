# Optional: residential / datacenter proxy. Rotating exit IPs improve the
# "managed" branch's chance of recovering on retry.
PROXY_URL=

# Optional: solver API key for the give-up branch.
SOLVER_API_KEY=
