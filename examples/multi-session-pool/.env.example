# Number of concurrent sessions. Each ≈ 150–300 MB resident; cap empirically.
POOL_SIZE=4

# Stable identifier mixed into the seed so reruns within a job are
# reproducible while cross-job runs are independent.
RUN_ID=local-dev
