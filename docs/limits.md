# Limits — what mochi does not cover

> Moved. The canonical location is the docs site at
> [`docs/content/docs/reference/limits.md`](content/docs/reference/limits.md),
> rendered at <https://mochijs.com/docs/reference/limits>.
>
> This file is kept as a stub so that historical references (PRs, commits,
> external links) resolve to a pointer rather than a 404. Edit the canonical
> file; this stub is not the source of truth.

The Limits document is the architectural-honesty page. Every fingerprint
vector we know we don't cover lives there with a root cause, an affected-probe
list, a mitigation, and a tracking link. Per invariant **I-8**, new gaps
discovered must be added in the same PR that creates them.

Read it before opening an issue saying "X site detects mochi" — half the
answers are already there.
